services:
  - type: web
    name: rasterize-render
    env: node
    rootDir: render
    # No font packages needed — NotoSans-Subset.ttf is bundled via build step.
    buildCommand: "npm install && node ../scripts/build.mjs render"
    startCommand: npm start