services:
  - type: web
    name: rasterize-render
    env: node
    rootDir: render
    # No font packages needed — JakartaSans-Regular.ttf is bundled via build step.
    buildCommand: "npm install && node ../scripts/build.mjs render"
    startCommand: npm start